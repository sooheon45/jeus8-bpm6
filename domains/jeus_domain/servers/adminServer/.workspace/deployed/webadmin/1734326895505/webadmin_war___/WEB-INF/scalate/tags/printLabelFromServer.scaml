- import jeus.tool.webadmin.config.ServerInfo

-@ val serverInfos: List[ServerInfo]

-@ val name: String

- 
	def printLabelFrom(server: String): String = {
		serverInfos.find(_.name == server).map { info =>
			s"${server} -- ${info.status}"
		} getOrElse (server)
	}
= printLabelFrom(name)
