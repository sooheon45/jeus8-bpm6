-@ val selected: Int = 0
-@ val disabled: Boolean = false

- val url = Utils.buildUri("/domain")
%ul.tab01
	-
		J.render("tab", Map(
			"items" -> List(
				TabItem(url, "message:menu.basic"),
				TabItem(url + "/lifecycle", "message:menu.common.lifecycle")
			),
			"selected" -> selected
		))
